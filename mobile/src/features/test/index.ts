
/* 
    Created by thaolt
*/

import TestContainer from 'features/test/view/test.screen';
import { connect } from 'react-redux';
import { Dispatch } from 'redux';

const mapStateToProps = (state: any) => ({

})

const mapDispatchToProps = (dispatch: Dispatch) => ({
   
});

export const TestScreen = connect(mapStateToProps, mapDispatchToProps)(TestContainer)


