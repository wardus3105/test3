
/* 
    Created by tuPC
*/

import TestUiContainer from 'features/test-ui/view/test-ui.screen';
import { connect } from 'react-redux';
import { Dispatch } from 'redux';

const mapStateToProps = (state: any) => ({

})

const mapDispatchToProps = (dispatch: Dispatch) => ({
   
});

export const TestUiScreen = connect(mapStateToProps, mapDispatchToProps)(TestUiContainer)


